import React from "react";
import UnifiedModuleBox from "./UnifiedModuleBox";

const COTModuleBox = (props) => (
  <UnifiedModuleBox {...props} title="COT Module" color="#facc15" />
);

export default COTModuleBox;
